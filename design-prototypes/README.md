# Hanimo design prototypes

These files are standalone visual explorations. They are not linked to the
production application and must not be treated as shipped feature claims.

- `open-source-hub.html`: bilingual open-source trust and onboarding direction
  using the Hanimo amber/stone design system.

Open the HTML directly in a browser. No build step is required.
